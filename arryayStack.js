// STACK

/// functions push, pop, peek, length

var letters = [];

var word = 'racecar'

var rword = [];

for(var i = 0; i <= word.length; i++ ){
    letters.push(word[i])

}

for (var i = 0; i <= word.length){
    rword.pop(word[i])
}
